[project]
name = "a-setup-finder-company"
version = "0.1.0"
description = "Rule-based MGC futures signal system with DXY and prop-risk controls"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
# Live Tradovate feed only. Core strategy + backtests + replay need no deps.
live = ["websocket-client>=1.7"]

[tool.unittest]
start-directory = "tests"
